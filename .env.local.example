# Port the dashboard itself listens on (the Next.js dev/start server).
# Default 3000 if unset. Copy to .env.local and adjust as needed.
#
# All OTHER knobs (rate limiting, scrape cadence, arbitrage/ducats filters)
# live in config.yaml - see config.yaml.example.
PORT=3000
